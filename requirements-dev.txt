# Dev / evaluation dependencies for hayamimi -- accuracy benchmarking against
# faster-whisper, test dataset synthesis (TTS), test video download, and
# the test suite. Not needed to run realtime_transcribe.py itself.
-r requirements.txt

faster-whisper==1.2.1
jiwer==4.0.0
edge-tts==7.2.8
yt-dlp==2026.8.19
pytest==9.1.1
OpenCC==1.4.2
pykakasi==2.3.0   # kana-space scoring for the singing eval (eval_singing.py)
gdown==6.1.0      # PJS corpus download in make_singset.py
pyarrow==25.0.1   # column-projected remote parquet reads (eval_translate.py, FLEURS)
sacrebleu==2.6.0  # chrF scoring (eval_translate.py)
